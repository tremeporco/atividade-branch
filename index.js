// 1. Soma
const somar = (a, b) => a + b;

// 2. Subtração 
const subtrair = (a, b) => a - b;

// 3. Multiplicação
const multiplicar = (a, b) => a * b;

// 4. Divisão 
const dividir = (a, b) => {
    if (b === 0) return "Erro: Divisão por zero";
    return a / b;

    